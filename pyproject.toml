[project]
name = "uae-business-setup-advisor"
version = "0.1.0"
description = "Evidence-backed UAE business setup advisor — every claim bound to a source record."
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
