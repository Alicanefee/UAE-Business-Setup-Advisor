# Runtime
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pyyaml>=6.0
python-dotenv>=1.0

# Optional: LLM fallback for the classifier (only used when OPENAI_API_KEY is set)
# openai>=1.50

# Tests
pytest>=8.0
httpx>=0.27
